# 태스크

## 1. 공용 키프레임

- [x] 1.1 `index.css`에 `sheet-in`(아래에서 올라오기) · `fade-in` · `settle-in`(내려앉으며 나타나기)
- [x] 1.2 reduced-motion 전역 블록이 이미 `*`를 훑고 있어 새 연출도 함께 멈추는지 확인 (D1)

## 2. 바텀시트

- [x] 2.1 패널에 `animate-sheet-in`, 딤에 `animate-fade-in`
- [x] 2.2 닫힘 연출은 넣지 않는다 — 언마운트를 늦춰야 해서 얻는 것에 비해 상태가 는다 (D6)

## 3. 진행 바

- [x] 3.1 `ui/ProgressBar` — 채움을 트랙 폭 전체로 두고 `translateX`로 민다 (D2)
- [x] 3.2 빈 상태에서 시작해 값으로 간다 — 마운트할 때 최종값이면 전환이 걸릴 구간이 없다
- [x] 3.3 `MacroProgress`가 쓰도록 교체. 목표가 없으면 빈 트랙만 둔다
- [x] 3.4 `WeightProgressCard`가 쓰도록 교체 — 여기만 `transition-[width]`라 규칙에서 벗어나 있었다
- [x] 3.5 테스트 5개 — 값별 위치·범위 밖 값 자르기·끝 모양이 눌리지 않는 방식인지

## 4. 칼로리 링

- [x] 4.1 `strokeDasharray`를 100으로 고정하고 `strokeDashoffset`을 전환한다 (D3)
- [x] 4.2 빈 링에서 값까지. 홈에 들어올 때마다 도는 것을 받아들인다 — 한 번만 돌게 하면
      StrictMode 이중 마운트에서 개발 중에만 연출이 사라진다

## 5. 밑줄 탭

- [x] 5.1 탭마다 그리던 밑줄을 **하나로** 바꿔 `translateX`로 옮긴다 (D4)
- [x] 5.2 껍데기(폭 `100/n%`)와 안쪽(`mx-2`)을 나눈다 — 여백을 껍데기에 주면 칸이 어긋난다
- [x] 5.3 선택이 목록에 없으면 그리지 않는다 — 0번 자리에 밑줄이 남는다
- [x] 5.4 테스트 4개 — 위치·하나뿐인지·없는 선택·기존 동작(선택 알림·클릭)

## 6. 저장 피드백

- [x] 6.1 방금 담긴 기록 줄에 `animate-settle-in` — 직전 목록에 없던 id로 가른다 (D5)
- [x] 6.2 날짜를 ref에 함께 들고 있는다 — id만 들면 날짜 이동 때 전부 움직인다
- [x] 6.3 체중 "오늘 기록됨" 배지에도 같은 연출
- [x] 6.4 테스트 3개 — 첫 목록·담은 직후(그 줄만)·날짜 이동
- [x] 6.5 **헛통과 잡음** — 날짜 이동 테스트가 음식 목록의 같은 이름에 걸려 가드를 빼도 통과했다.
      기록 줄에만 있는 문구로 바꾸고 가드를 다시 빼서 실패하는 것까지 확인

## 7. 버튼

- [x] 7.1 `ui/form`의 `Button`에 `transition-transform active:scale-[0.98]` (D7)
- [x] 7.2 체중 저장 버튼 — 공용 부품을 안 쓰는 자리 중 가장 자주 눌린다

## 8. 추세선

- [x] 8.1 `sweep-x` — 도판을 가리는 사각형을 가로로 편다. 선 길이를 재지 않아 선·점·면이 한 덩어리로 드러난다 (D8)
- [x] 8.2 기준선은 클립 밖 — 도판의 틀이라 처음부터 있어야 한다
- [x] 8.3 범위를 바꾸면 다시 그린다 — 클립 사각형의 `key`에 좌표를 넣어 갈아끼운다
- [x] 8.4 `clipPath` id는 `useId()` — 한 문서에 그래프가 둘 이상이면 고정 id는 서로를 문다
- [x] 8.5 홈 체중 미니 그래프에도 같은 처리 — 하나만 움직이면 같은 그래프가 다르게 논다

## 9. 마무리

- [x] 9.1 `npm test` · `npm run build` · `openspec validate --strict` 통과
- [ ] 9.2 **실기기 확인** — 시트가 올라오는 속도, 링·바·추세선이 자라는 시간이 답답하지 않은지.
      움직임 줄이기를 켠 상태도 함께 본다
