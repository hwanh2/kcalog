# Tasks: polish-ui-details

> 전부 프론트엔드. 백엔드·마이그레이션 없음.
> 완료 판정은 "고쳤다"가 아니라 **테스트·빌드 통과 + 해당 시나리오 확인**이다.

## 1. 홈 촬영 유도 카드 (요청 4)

- [x] 1.1 죽은 링크 수정 — `/meals/new`(없는 라우트) → `/records?camera=1`
- [x] 1.2 끼니 라벨을 시각 기준(`defaultMealType`)으로 (D1)
- [x] 1.3 `mealSuggest.ts`·테스트 삭제 — 사용처가 사라진다 (D1)
- [x] 1.4 테스트 — 카드가 촬영 경로로 가고, 라벨이 시각 기준 끼니와 같다

## 2. 분석 결과의 끼니 변경 (요청 6)

- [x] 2.1 `AnalysisResultSheet`가 끼니 변경을 받아 위로 전달 (D2)
- [x] 2.2 `[점심 ▾] 에 [기록하기]` — 네이티브 `select` + 직접 그린 화살표 (D2)
- [x] 2.3 `AiRecordPanel`·`AddFoodPanel`·`RecordsPage`로 배선 — 시트와 탭 세그먼트가 같은 상태를 가리킨다
- [x] 2.4 테스트 — 고르면 부모에게 전달·선택값 표시·다섯 끼니 모두 선택 가능

## 3. 체중 입력 시작값 (요청 7)

- [x] 3.1 그날 기록이 없으면 `latestKg`를 입력값으로 채운다 (D3)
- [x] 3.2 그 값이 최근 기록값임을 알린다 — 그날 기록이 있으면 안 뜬다 (D3)
- [x] 3.3 테스트 — 기록 없는 날 채워짐·안내 표시, 기록 있는 날 안내 없음, 기록 0건이면 빈 칸, ±0.1이 이어짐

## 4. 전체 화면 로딩 (요청 1)

- [x] 4.1 `ui/LoadingScreen` — 로고·링·문구, `role="status"` (D4)
- [x] 4.2 `RequireAuth`·`CallbackPage`에 적용
- [x] 4.3 테스트 — 문구가 눈에 보이는 텍스트로 있고 `role="status"`로 읽힌다

## 5. 화면 제목·색 (요청 3·5)

- [x] 5.1 체중·리포트·AI PT의 `h1`을 `sr-only`로 (D5)
- [x] 5.2 `PhotoOverlay` 안내 배지를 `bg-brand-dark/90`으로 — 실제 사진 위 12종 비교 후 선택 (D6)
- [x] 5.3 `onBrand on brandDark`(3.63:1)를 `KNOWN_EXCEPTIONS`에 기록 — 로고·FAB에도 이미 쓰이던 조합인데 목록에 없었다
- [x] 5.4 테스트 — 탭 이름과 같은 제목이 눈에 보이지 않지만 `h1`으로는 있다

## 6. 사진첩에서 가져오기 (요청 추가)

- [x] 6.1 입력을 둘로 — 눌러서 여는 쪽은 `capture` 없이, FAB 경로만 `capture="environment"` (D7)
- [x] 6.2 라벨 문구를 "촬영 · 사진첩"으로
- [x] 6.3 테스트 — 각 입력의 `capture` 유무, FAB이 촬영 쪽을 연다

## 7. 리포트 조회 기간 (요청 추가)

- [x] 7.1 제목 자리에 `rangeStart`–`rangeEnd` 표시 — 화면이 따로 계산하지 않는다 (D8)
- [x] 7.2 테스트 — 범위 표시, 시작=끝이면 한 번만 적는다

## 8. 분석 대기 화면의 사진 크기 (요청 추가)

- [x] 8.1 `photoPreview.ts` — 사진 미리보기 크기를 한 곳에서 정한다 (D9)
- [x] 8.2 `AiRecordPanel`·`AnalyzingView`가 그 상수를 함께 쓴다 — 대기 화면만 `w-52`+`object-cover`라 작아지고 잘렸다
- [x] 8.3 테스트 — 대기 화면 사진이 공유 상수를 쓴다, 설명만 분석이면 사진을 안 그린다

## 9. 사진 위 배지가 화면 밖으로 나감 (요청 추가)

- [x] 9.1 중심이 22%/78% 밖이면 사진 끝에 붙인다 — 꼬리도 그 끝으로 (D10)
- [x] 9.2 가운데 맞춤 배지에 너비 상한 — 가까운 쪽 여백 × 2 (D10) ⚠️ 9.1만으로는 못 막는다
- [x] 9.3 이름의 `whitespace-nowrap` 제거 — 한 줄로 뻗어 상한을 무의미하게 만들었다
- [x] 9.4 테스트 5종 — 좌/우 끝 붙임, 가운데 유지, 두 가지 너비 상한, box 없으면 안 그림
- [x] 9.5 눈으로 확인 — 실제 사진에 배지 6개를 얹어 전/후 비교(사진 경계 밖으로 안 나감)

## 10. PR #39 리뷰 대응

- [x] 10.1 **홈 카드가 끼니를 쿼리로 넘긴다**(nit이지만 실제로는 **이 PR이 세운 MUST 위반**) — 두 화면이 각자 `new Date()`를 보면 끼니 경계에서 갈린다. `?camera=1&meal=DINNER` (D1)
- [x] 10.2 `RecordsPage`가 쿼리의 끼니를 받되 **아는 값만** — 주소창은 아무 값이나 담을 수 있다
- [x] 10.3 테스트 4개 — 카드가 실어 보냄·도착 화면이 받음·모르는 값 무시·끼니 없이 들어오면 시각
- [x] 10.4 `PhotoOverlay`의 네 갈래 분기를 `horizontalPlacement` 하나로 — 한 곳만 고치면 배지와 꼬리가 어긋난다 (D10)

## 11. 마무리

- [x] 11.1 `npm test`(315) · `npm run build` · `openspec validate --strict` 통과
- [x] 11.2 구현 중 설계와 어긋난 결정을 design.md에 반영 — 세그먼트→드롭다운 선회, 갈색→브랜드 오렌지 선회, "개인 코칭" 라벨 동반 삭제, `tsc --noEmit`이 테스트를 안 본다는 것
- [ ] 11.3 **실기기 확인** — `fix-record-ux`에서 이월(바텀시트 안전 영역·탭 지연·포커스 링) + 이번 로딩 화면 깜빡임 + **사진첩 선택지가 실제로 뜨는지**(에뮬레이터로는 안 잡힌다)

## 범위 밖 (기록만)

- `role="tab"` 위반 2곳(`ReportPage`·`WeightPanel`) — 스펙 MUST NOT 위반이지만 이번엔 두기로 했다(design Risks 참조)
