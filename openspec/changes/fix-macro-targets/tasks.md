# 태스크

## 1. 계산식 (TDD)

- [x] 1.1 `MacroTargetGTest` 재작성 — **실패하는 테스트 먼저**. 순수 계산이라 TDD 대상(AGENTS.md)
      - 기본 비율(탄 55/단 20/지 25) · 근력 비율(탄 45/단 30/지 25)
      - 단백질 상한(체중×2.0) · 하한(체중×1.2) · 범위 안(자르지 않음)
      - 남은 칼로리가 탄수로 흘러가 **세 값의 kcal 합이 목표와 일치**(8×5×2=80조합 순회)
      - 체중 null → 범위 제한 없이 비율만 (D7)
      - 칼로리 목표 null → 세 값 모두 null
- [x] 1.2 `MacroTargetG.from(kcalTarget, weightKg, strengthTraining)` 구현 — 단백질·지방을 g으로 먼저 반올림하고
      나머지에서 탄수를 역산한다(D11). 반올림 전 값으로 나머지를 구하면 화면의 합이 목표와 어긋난다

## 2. 근력 운동 여부 저장

- [x] 2.1 `V20__member_muscle_goal.sql` — `BOOLEAN NOT NULL DEFAULT false`. 기존 회원은 백필하지 않는다 (D3)
- [x] 2.2 `Member.muscleGoal` 필드 + `completeOnboarding`·`updateProfile` 시그니처
- [x] 2.3 `OnboardingRequest`·`MemberResponse`·`KcalSuggestionRequest` 필드 추가.
      `UpdateMemberRequest`·`Member.updateProfile`에는 **넣지 않는다** — 프로필 토글을 빼면서
      호출부가 사라졌고, 호출되지 않는 API 필드를 남기지 않는다 (D3)
- [x] 2.4 **primitive `boolean`이 아니라 `Boolean`으로 받는다** — Jackson은 누락된 primitive를 null로 보고
      거절해서, 이 필드를 모르는 구버전 앱(PWA 캐시)의 요청이 400이 됐다. 통합 테스트 11개가 잡았다.
      `strengthTrainingOrDefault()`로 누락을 false로 읽는다

## 3. 호출부 배선

- [x] 3.1 `DashboardService` — `WeightLogRepository` 주입, 최근 체중 + 회원의 근력 여부를 넘긴다 (D8)
- [x] 3.2 `ReportService` — 이미 조회하던 `latest`를 위로 올려 macro 계산과 cut 판정이 같은 값을 쓰게 했다
- [x] 3.3 `KcalSuggestionResponse.of` — 요청의 체중·근력 여부를 받아 넘긴다.
      온보딩에서 본 숫자와 대시보드 숫자가 어긋나면 안 된다

## 4. 프론트

- [x] 4.1 `api/member` 타입 — `strengthTraining` 추가
- [x] 4.2 온보딩 **Step 4(목표)** 방향 선택 아래에 토글 — 활동량 옆에 두었다가 옮겼다.
      방향(체중이 어디로)과 나란히 놓여야 토글(그 체중의 내용)과의 차이가 읽힌다 (D4)
- [x] 4.3 `features/dashboard/MacroGuideSheet` 신규 — 기존 `ui/Sheet` 재사용. 툴팁 컴포넌트가 없고
      모바일 hover는 접근성이 나쁘다. 목표 숫자가 보이는 세 화면이 함께 쓰므로 dashboard에 둔다 (D9)
- [x] 4.4 **홈 `MacroProgress`("오늘의 탄·단·지 달성도") 헤더에 (i)** — 목표를 매일 마주하는 자리이고
      오해도 거기서 생긴다. 문구는 상시로 깔지 않고 버튼만 둔다 (D9)
- [x] 4.5 온보딩 Step 5(결과)에 (i) 버튼
- [x] 4.5a **결과 화면에 근거 세 줄** — 유지 칼로리가 무엇인지, 목표가 방향에서 얼마나 조정됐는지,
      탄단지를 무엇 기준으로 잡았는지. 단백질 g/kg는 실제 목표 g에서 되짚어 계산해
      상·하한에 잘려도 어긋나지 않게 (D19)
- [x] 4.5a-1 **아래에 "왜" 섹션** — 유지 칼로리를 알면 스스로 조절할 수 있다는 것, 단백질→지방→탄수
      순서로 정한 이유, 탄수 20g ↔ 지방 9g으로 옮길 수 있다는 것. 숫자 옆 한 줄이 "무엇"이면
      이쪽이 "왜"다. 접지 않는다 — 스크롤해야 닿아 급한 사람은 그냥 시작한다 (D19)
- [x] 4.5a-1b **"매일 기록해야 정확해져요"를 설명 맨 위에** — 지금 숫자는 추정이고 매일 남겨야
      실측으로 바뀐다는 것. 이 앱에서 가장 중요한 한 가지라 색 있는 카드로 층을 나눈다.
      체중 재는 시각(아침·화장실 뒤)까지 적어야 추세가 흔들리지 않는다 (D19)
- [x] 4.5a-1c **설명 글 가독성** — 줄간격 1.75, 제목 `text-[15px] font-bold`로 크기 위계,
      본문 `text-muted` → `text-ink/75`, 항목 여백 `py-4`. 대비가 아니라 밀도가 문제였다 (D22)
- [x] 4.5a-2 목표 섭취량 입력 폭을 값 길이에 맞춘다(`ch` + `tabular-nums`) — `w-32` 고정이라
      네 자리보다 짧으면 `kcal`이 멀리 떨어져 있었다 (D21)
- [x] 4.5a-3 **위저드 하단을 `sticky bottom-0`으로** — 설명이 길어져 '시작하기'가 스크롤 아래로
      밀렸다. 접거나 (i)로 숨기는 대신 버튼을 붙인다. `sticky`는 흐름에서 자리를 차지해 본문
      마지막 줄을 가리지 않는다. 배경 채움 + 위쪽 그라데이션 한 겹 (D23)
- [x] 4.5b **결과 화면의 (i) 두 개를 뺀다** — 처음 보는 자리라 누를 사람이 적고, 아이콘이 붙으면
      홈 화면과 구분이 안 된다. 근거는 화면에 직접 적고 문장은 한 줄에 한 가지만 (D19)
- [x] 4.5c **플랜을 만드는 화면(단계당 1.5초 · 합 4.5초)** — 계산이 순식간이라 결과가 튀어나오면
      답한 것들이 쓰였는지 알 수 없다. `Promise.all([요청, 타이머])`로 최소 시간만 보장한다 (D20)
- [x] 4.5c-1 **접시에 한 조각씩 담기는 그림** — 단계가 넘어갈 때마다 탄·단·지 색 조각이 떨어진다.
      그림 자체가 진행이라 도는 링도 점 세 개짜리 진행 표시도 뺐다. 세 번 갈아엎은 끝이다
      (이모지 1개 → 이모지 3종 → 선 아이콘 3종 → 접시). 접시는 위에서 본 시점(`MealArt`와 같다) (D20)
- [x] 4.5c-2 **결과 화면의 단계 표시도 감춘다** — `WizardShell`에 `showSteps`를 두어 "STEP 5"·
      "5/5"·진행 막대를 뺀다. 마지막 화면은 더 물을 것이 없어 진행 표시가 할 말이 없다.
      뒤로가기·CTA는 남긴다 (D20)
- [x] 4.5d **만드는 동안 위저드 셸을 벗는다** — 단계 번호·뒤로가기·진행 막대·CTA는 손댈 수 없는
      장식이고 시선을 뺏는다. 셸 안에서 CTA만 비활성화했더니 더 어수선했다 (D20)
- [x] 4.5e `PLAN_BUILD_MS`를 내보내고 **테스트에서 시간만 20ms로** — 그대로 기다리면 온보딩
      테스트가 길어진다(2초씩 쓰던 때 12초). 화면은 진짜를 그린다
- [x] 4.6 `NutritionTargetCard`에 (i) 버튼. **근력 토글은 두지 않는다** — 보는 카드에 스위치가 섞이면
      성격이 흐려진다. 온보딩에서만 받는다 (D3)
- [x] 4.7 `ProfileEditSheet`도 제안 조회에 근력 여부를 싣는다 — `tsc -b`가 잡았다(`npx tsc --noEmit`은 못 잡음)
- [x] 4.8 **`CalorieGuideSheet` 신규 + 칼로리 링 헤더에 (i)** — 유지칼로리가 기준선이라는 것, 조절 폭과
      결과(7700kcal/kg), 증량이 감량과 대칭이 아니라는 것, 2주 기록하면 추정값이 실측으로 바뀐다는 것,
      체중이 하루에도 흔들린다는 것. 탄단지 시트와 합치지 않는다 — 답하는 질문이 다르다 (D12)
- [x] 4.9 **두 시트의 위계 정리** — 같은 회색 카드를 쌓았더니 읽히지 않았다. 결론(제목) → 근거 숫자
      (`text-2xl`·`tabular-nums`) → 보조 설명 → 각주로 갈랐다. "한 달"은 큰 숫자와 같은 줄에 둔다.
      증량 카드는 감량과 색을 달리 써 대칭으로 읽히지 않게 한다 (D13)
- [x] 4.10 **칼로리 카드 전체를 리포트 링크로** — 헤더의 "리포트 ›"를 떼어 두 카드의 (i)를 같은
      자리(모서리에서 8px)에 맞춘다. 링크로 감싸지 않고 형제로 깔아 면만 덮고 (i)는 `z-10`.
      브라우저 히트 테스트로 확인했다 — jsdom은 stacking을 몰라 이 회귀를 못 잡는다 (D14)

## 4b. 온보딩 신체 정보 입력

- [x] 4b.1 `SliderField` → `StepperField` — 드래그를 걷어내고 −/+ 버튼(44px) + 가운데 큰 숫자 입력.
      버튼은 범위를 넘지 않고, 직접 입력은 막지 않는다(타이핑 중간에 걸리지 않게) (D15)
- [x] 4b.2 몸무게만 `inputMode="decimal"` — 나머지는 정수다
- [x] 4b.3 **나이를 세는나이로** — 2002년생이면 25세. 기존은 `연도 − 나이`라 25가 2001년생이 됐다
- [x] 4b.4 프로필 카드·편집 시트의 나이 표시도 같은 환산으로 — 안 맞추면 입력한 25가 24로 보인다.
      `lib/age` 한 곳에 두고 셋이 공유한다
- [x] 4b.5 서버 BMR 계산은 그대로 둔다 — 만 나이 공식이라 세는나이를 넣으면 한 살 많아진다 (D15)
- [x] 4b.6 `lib/age` 테스트 4개 — 2002→25 · 25→2002 · 왕복 · 그 해 출생
- [x] 4b.7 **성별별 시작값** — 남 174/74, 여 162/58, 나이 25. 키는 20~30대 평균(건강검진통계)을
      반올림했고 **체중은 근사치**다(통계 표를 직접 확인하지 못함). 어차피 바로 고치는 시작점이다
- [x] 4b.8 이미 고친 값은 성별을 바꿔도 덮지 않는다(`bodyTouched`) — 가드를 빼서 실패하는지 확인했다
- [x] 4b.9 남성 몸무게 시작값 75kg
- [x] 4b.10 **방향과 목표 체중이 어긋나면 안내** — 감량인데 더 무겁거나 증량인데 더 가벼울 때.
      막지 않고 `role="status"`로 알리기만 한다. 지금 체중을 함께 보여 무엇을 고칠지 알게 한다 (D17)
- [x] 4b.11 **한국어 줄바꿈을 `body`에서 잡는다** — `word-break: keep-all` + `overflow-wrap: break-word`.
      "감량"이 "감/량"으로 쪼개지고 "요."만 다음 줄로 떨어지고 있었다. 앱 전체가 한국어라 전역이다 (D18)

## 5. 문서

- [x] 5.1 `DashboardResponse`·`MacroTargetG` javadoc의 50/30/20 주석
- [x] 5.2 `openspec/specs/`는 **직접 고치지 않는다** — `openspec archive`가 델타를 본 스펙에 반영한다.
      "50/30/20"이 박혀 있는 `daily-dashboard`·`onboarding`은 이 change의 델타로 덮인다

## 6. 마무리

- [x] 6.1 통합 테스트 — 기존 기대값 갱신 + **대시보드가 weight_log를 실제로 읽는지** 2개 추가(상한·하한).
      이 배선이 빠지면 체중을 알면서도 범위가 적용되지 않아 고치려던 상태 그대로 남는다
- [x] 6.2 프론트 테스트 6개 — 온보딩 토글 전송(제안·제출 양쪽)·기본 false·탄단지 안내 3곳(홈·온보딩·프로필)·
      유지칼로리 안내(표의 숫자까지 확인). 온보딩 전송 필드를 **빼서 실패하는지 확인**했다
- [x] 6.3 `./gradlew test`(275) · `npm test`(444) · `npm run build` · `openspec validate --strict` 통과
- [ ] 6.4 실기기 확인 — 온보딩 토글이 결과 화면 숫자를 실제로 바꾸는지, 홈·온보딩·프로필의 (i)가
      모두 열리는지, **칼로리 카드의 (i)를 눌렀을 때 리포트로 새지 않는지**(jsdom이 못 잡는 겹침),
      카드의 다른 곳을 누르면 리포트로 가는지, **플랜 만들기가 실제로 4.5초를 채우는지**(테스트는
      시간을 줄여 돌므로 이것만은 실기기로 본다), **붙인 하단 버튼이 다른 단계에서도 어색하지 않은지**

## 7. 이름 바로잡기 (근력 운동 → 근육량 목표)

- [x] 7.1 묻는 것을 **습관이 아니라 목표로** 바꿨다 — "근력 운동을 하는가" → "근육량도 목표인가".
      목표 단계의 다른 질문(감량·유지·증량)과 같은 층위가 된다 (D3)
- [x] 7.2 **마이그레이션 컬럼명까지 맞춘다** — `strength_training` → `muscle_goal`.
      배포 전이라 지금이 비용 0인 시점이고, 나중이면 새 버전 파일이 필요하다
- [x] 7.3 `Goal.BULK` 라벨 "근육 증량" → **"체중 증량"** — 둘 다 "근육"이면 무엇이 다른지 알 수 없다
- [x] 7.4 백엔드 필드·메서드·주석, 프론트 타입·컴포넌트(`MuscleGoalField`)·문구 일괄 정리
- [x] 7.5 `./gradlew test`(275) · `npm test`(457) 통과
