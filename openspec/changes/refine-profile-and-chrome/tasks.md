# Tasks: refine-profile-and-chrome

> 사용자가 준 7건. 번호는 요청 순서 그대로 둔다 — 추적이 끊기지 않게.
> 완료 판정은 "다 했다"가 아니라 **테스트·빌드 통과 + 해당 화면 확인**이다.

## 1. 여백 (요청 ①)

- [x] 1.1 `WeightPanel` 첫 섹션에서 `mt-4` 제거 — 셸의 `pt-4`와 겹쳐 32px이었다
- [x] 1.2 `CoachPage`의 `Briefing` 첫 요소에서 `mt-4` 제거
- [x] 1.3 뒤따르는 카드 간격은 건드리지 않는다 (D8)

## 2. 프로필 (요청 ②)

### 2a. 백엔드 — 출생연도·성별 수정 허용

- [x] 2.1 `UpdateMemberRequest`에 `birthYear`·`gender` 추가. 출생연도는 `@Min(BIRTH_YEAR_MIN)`
- [x] 2.2 `Member.updateProfile`에 두 필드 — null이면 안 건드리는 규칙 유지
- [x] 2.3 `MemberService.updateProfile`에 미래 출생연도 차단 (`validateBirthYear` 재사용) — **수정 경로를 열면 그 구멍도 열린다** (D1)
- [x] 2.4 통합 테스트 — 출생연도 수정·성별 수정·미래 연도 400·부분 수정(안 보낸 필드 유지)

### 2b. 프론트 — 프로필 편집 시트

- [x] 2.5 `api/member.ts`의 `UpdateMemberRequest`에 두 필드
- [x] 2.6 `ProfileEditSheet`에 출생연도(숫자)·성별(선택) 입력. 라벨은 "출생연도", 아래에 계산된 만 나이 표기 (D1)
- [x] 2.7 제안 칼로리 조회가 **편집 중인** 성별·출생연도를 쓰도록 — 저장값을 쓰면 방금 고친 값이 계산에 안 들어간다
- [x] 2.8 테스트 — 미리 채움·수정 PATCH·안 바꾼 필드 미포함·만 나이 표시

### 2c. 프론트 — 유지칼로리 다시 계산

- [x] 2.9 `features/tdee/TdeeSummary` 추출 — `TdeeCard`와 새 시트가 같은 본문을 쓴다 (D4)
- [x] 2.10 `features/profile/TdeeRecalcSheet` — 열 때 `['tdee']` 무효화 (D4)
- [x] 2.11 `NutritionTargetCard`의 `✎ 조정` → `유지칼로리 다시 계산`, 이 시트를 연다
- [x] 2.12 설정 메뉴 "프로필 편집"은 그대로 편집 시트 — **두 진입점이 갈라졌는지 테스트로 고정** (D3)
- [x] 2.13 테스트 — 시트 표시(실측/공식 배지)·적용·같은 목표일 때·데이터 부족

## 3. 추세 범위 라벨 (요청 ③)

- [x] 3.1 `TrendRange` 값을 `'1주' | '1개월' | '3개월'`로 (D6). `RANGE_DAYS` 일수는 그대로
- [x] 3.2 `WeightPanel`의 `RANGES`·`estimator.test.ts` 갱신

## 4. 헤더 (요청 ④)

- [x] 4.1 `AppShell` 헤더에서 부제("AI 식단 · 탄단지 코칭") 제거
- [x] 4.2 `kcalog.ai` → `kcalog`
- [x] 4.3 테스트 — 헤더에 부제가 없고 `.ai`가 없다

## 5. 리포트 날짜 구분자 (요청 ⑤)

- [x] 5.1 `ReportPage.formatRange`의 `–` → `~`. 스펙 델타는 두지 않는다 (D9)
- [x] 5.2 테스트 갱신

## 6. 확대·축소 잠금 (요청 ⑥)

- [x] 6.1 `index.html` viewport에 `maximum-scale=1, user-scalable=no`
- [x] 6.2 `index.css`에 `touch-action` — 더블탭 확대 차단
- [x] 6.3 `main.tsx`에 `gesturestart` 차단 — **iOS Safari는 메타를 무시한다** (D5)
- [x] 6.4 접근성 손실을 design.md에 남긴다 (D5) — WCAG 1.4.4 상충

## 7. 즐겨찾기 검색 제거 (요청 ⑦)

- [x] 7.1 `SearchField`를 `FoodList`에서 `AddFoodPanel`로 올린다 (D7)
- [x] 7.2 `tab !== 'favorite'`일 때만 그린다 — "자주 먹는"에는 남긴다
- [x] 7.3 테스트 — 즐겨찾기 탭에 검색 없음·자주 먹는 탭에 검색 있음

## 8. 후속 지적 3건

- [x] 8.1 "제안 적용"이 버튼으로 안 보인다 — 문장 속 ghost(회색 글자)를 면 위 primary 버튼으로 (D10)
- [x] 8.2 재계산 시트에 **계산 근거** 설명 — 실측/공식/데이터 부족 각각 다른 문구. 서버 정책 상수는 옮겨 적지 않는다 (D11)
- [x] 8.3 세트 담기를 **+ 버튼**으로 — 음식 목록과 같은 모양. 휴지통은 그대로 옆에 (D12)
- [x] 8.4 테스트 3개 — 실측 근거·공식 근거·+ 버튼(줄 전체가 버튼이 아님)
- [x] 8.5 세트 줄에서 휴지통과 `+`의 순서를 바꾼다 — 두 목록의 `+`가 같은 세로줄에 서게. 안쪽 여백도 `p-2.5`로 맞춘다
- [x] 8.6 사진 카드 버튼 문구를 `촬영`으로 — 동작(OS 선택지)은 그대로다

## 9. 홈 식사 줄 · 공통 기록 버튼

- [x] 9.1 홈 `MealCard`에 사진 없을 때 식기 아이콘 자리표시 — 음식기록 탭과 같은 아이콘 (D14)
- [x] 9.2 끼니 칩 — 색으로 가르지 않는다(탄단지 칩과 섞인다). ~~이모지 표식~~은 9.8에서 뺐다
- [x] 9.3 `features/meal/RecordButton` 추출 — `useId()`로 select id (D13)
- [x] 9.4 네 자리에 적용 — 분석 결과·음식 담기(수량)·직접 입력(record 모드)·세트 담기
- [x] 9.5 `onMealTypeChange`를 `FavoriteMealSection` → `FavoriteMealApplySheet`까지 내려보낸다
- [x] 9.6 즐겨찾기 저장은 제외 — 끼니에 담는 게 아니라 라이브러리에 넣는 것이다
- [x] 9.7 테스트 — 자리표시·끼니 칩·시트에서 끼니 변경 + 기존 `"…에 기록하기"` 단언 갱신(4개 파일)
- [x] 9.8 홈 목록을 **끼니 순서 → 시각순**으로 정렬 (`sortByMealOrder`). 이모지 표식 제거 (D15)
- [x] 9.9 `AppShell`에서 화면 전환 시 스크롤 맨 위로 — 화면마다 넣으면 빠뜨린다 (D16)
- [x] 9.10 본문 하단 여백에 `env(safe-area-inset-bottom)` 반영 — 노치 기기에서 내비가 마지막 카드를 덮었다 (D17)
- [x] 9.11 AI PT 대화 상대 이름 `코치 미아` → `AI 코치`
- [x] 9.12 테스트 2개 — 뒤늦게 채운 아침이 위로 옴·화면 전환 시 스크롤 초기화
- [x] 9.13 🔴 **배포 차단 — 옛 홈 화면 아이콘이 랜딩으로 떨어진다.** `LandingRoute`에서 standalone이면 `/app`으로 넘긴다 (D22)
- [x] 9.14 테스트 2개 — standalone이면 앱으로·브라우저면 소개 그대로. `add-landing-install-guide` 7.5의 "재설치" 전제도 함께 고쳐 적는다

## 10. 음식기록 화면 마감 · 빈 상태

- [x] 10.1 홈 끼니 칩을 브랜드 톤으로 (`bg-brand-soft`/`text-brand-ink`) — 회색은 존재감이 없었다
- [x] 10.2 음식기록 탭에서 앱 헤더를 감춘다 (D18)
- [x] 10.3 끼니 세그먼트를 **밑줄 탭**(`UnderlineTabs`)으로 — 활성 표시는 브랜드 색 (D18)
- [x] 10.4 빈 상태를 즐겨찾기와 같은 모양으로 (`EmptyMeals` 신설) — 한 줄 문구 + 큰 카드 제거 (D19)
- [x] 10.5 `ConfirmSheet` 실행 버튼에 `danger` 변형 — `tokens.ts`에 면·PAIR 추가 (D20)
- [x] 10.6 빈 상태 일러스트 — 음식기록은 식판, 즐겨찾기는 접시+포크·나이프로 소재를 가른다
- [x] 10.7 포크·나이프를 lucide 비율로 다시 그린다. 가운데 갈래가 목까지 닿게, 칼날은 하단 내비 아이콘과 같은 형태
- [x] 10.8 **가로 겹침 수정** — 접시 rx=40이라 나이프 칼날과 물렸다. 폭을 나눠 잡는다(포크 20~38 · 접시 50~110 · 나이프 120~138)
- [x] 10.9 일러스트 투명도를 두 번 낮춘다(×0.62 → ×0.65) — 처음엔 `border`라 안 보였고, `muted`로 바꾸니 진했다
- [x] 10.10 테스트 3개 — 음식기록 탭 헤더 없음·빈 상태 문구·기존 단언 유지
- [x] 10.11 끼니 합계 줄 **줄바꿈 수정** — 라벨·칼로리·칩 셋이 한 줄에 안 들어가 전부 접혔다("저녁 합/계"). 두 줄로 나누고 각 덩어리는 `whitespace-nowrap`
- [x] 10.12 기록 줄 높이 축소 — ☆·🗑을 세로 스택에서 가로로 눕힌다(오른쪽 칸 110px → 64px). 44px 탭 대상은 유지, 카드 패딩 `p-3` → `px-3 py-2`
- [x] 10.13 식판 아이콘 상하 반전 — 얕은 반찬 칸이 위, 밥·국 칸이 아래(실제 식판 배치)

## 11. 날짜별 보기 · 아이콘 교체

- [x] 11.1 `RecordsDateHeader` — 브랜드 면에 `8.15 토 ▾` + 주간 띠(월~일). 알림 자리는 만들지 않는다 (D21)
- [x] 11.2 날짜를 주소가 든다(`?date=`). 오늘이면 파라미터를 붙이지 않는다 — 05시 경계 동기화가 따라와야 한다
- [x] 11.3 주소로 들어온 날짜 검증 — 형식·**미래 날짜 차단**
- [x] 11.4 **보고 있는 날짜로 담는다** — `recordItems`가 `todayServiceDate()`로 덮어쓰던 것을 걷어낸다
- [x] 11.5 홈의 `전체보기`·촬영 카드가 `date`를 실어 보낸다
- [x] 11.6 `lib/date`에 `weekDates`·`koreanWeekday` — 후자는 `estimator`에서 옮겨왔다(두 기능이 함께 쓴다)
- [x] 11.7 앱 헤더 프로필을 아이콘만으로, 날짜 머리와 같은 크기·자리. **프로필 화면에서는 감춘다**
- [x] 11.8 날짜 머리 간격 축소 — 탭 대상 44px는 유지하고 여백만 줄인다
- [x] 11.9 테스트 7개 — 날짜 6종 + 프로필 화면에서 아이콘 없음
- [x] 11.10 앱 아이콘 교체 — favicon(png)·icon-180(iOS 정사각)·192·512·maskable·앱 안 마크
- [x] 11.11 **maskable을 따로 만든다** — 링이 캔버스 89%까지 닿아 같은 파일을 쓰면 원형 마스크에서 잘린다
- [x] 11.12 SVG 파비콘 폐기 — 마크의 `k`가 글자라 원본 서체(Manrope) 없는 기기에서 다르게 그려진다
- [x] 11.13 `AppMark`로 인앱 로고 통합 — 헤더·로딩·랜딩·**온보딩**·로그인
- [x] 11.14 랜딩에 남아 있던 `kcalog.ai` → `kcalog` (앱 헤더는 4.2에서 이미 처리)

## 12. PR #42 리뷰 대응

- [x] 12.1 🔴 **05시 경계 보호 되살림** — `?date=` 리팩터가 담기 직전 서비스일 재읽기를 걷어냈다.
      화면을 열어둔 채 경계를 넘기면 `today`가 낡아 **전날 정오**로 저장된다(야식 사용자가 정확히 이 경우)
- [x] 12.2 오늘을 보고 있을 때만 적용 — 지난 날짜 담기는 그대로. `today`도 함께 갱신해 띠·상한·목록이 따라오게
- [x] 12.3 테스트 1개 — 가짜 시계로 04:59→05:01을 넘겨 담고, **수정을 되돌리면 실패하는 것까지 확인**
- [x] 12.4 app-shell 스펙 시나리오를 실제 첫 줄(날짜 머리)과 맞춤 — meal-logging 스펙과 모순이었다
- [x] 12.5 D14의 칩 토큰(`bg-track` → `bg-brand-soft`)을 "구현 이탈"에 반영
- [ ] 12.6 `UnderlineTabs`/`SegmentedTabs` 중복 — 리뷰도 "현 규모에선 그대로 둬도 무방". 세 번째 변형이 생기면 뼈대만 공유한다

## 13. 마무리

- [x] 13.1 `./gradlew test` · `npm test` · `npm run build` · `openspec validate --strict` 통과
- [x] 13.2 구현 중 설계와 어긋난 결정을 design.md "구현 이탈"에 반영
- [ ] 13.3 **실기기 확인** — 확대 잠금은 데스크톱에서 재현되지 않는다(핀치·더블탭). 이월된 항목과 함께 본다
