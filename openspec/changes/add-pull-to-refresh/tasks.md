# 태스크

## 1. 제스처

- [x] 1.1 `lib/usePullToRefresh` — 맨 위에서 아래로 당긴 거리를 저항 0.5로 따라간다 (D3)
- [x] 1.2 `touchmove`만 `passive: false` — 여기서 `preventDefault`를 해야 iOS 고무줄과 안 겹친다 (D5)
- [x] 1.3 진행 중 값은 ref, 그리는 거리만 state. 콜백도 ref로 — 의존성에 넣으면 제스처 도중 리스너가 다시 붙는다 (D6)
- [x] 1.4 시작하지 않을 세 경우 — 문서가 맨 위가 아닐 때·시트 안·자기 스크롤이 내려간 영역 (D4)
- [x] 1.5 도는 동안 다시 당겨도 겹쳐 부르지 않는다
- [x] 1.6 실패해도 표시는 걷는다 — 남으면 화면이 잠긴 것처럼 보인다

## 2. 표시

- [x] 2.1 `ui/PullIndicator` — 당긴 만큼 내려오고, 당길수록 또렷해진다
- [x] 2.2 임계점을 넘기면 색이 들고 화살표가 뒤집힌다 (문구 없이 "이제 놓으세요")
- [x] 2.3 도는 동안은 회전으로 바뀌고 `role="status"`로 알린다 — 보이는 표시는 장식이다 (D7)
- [x] 2.4 도는 중 자리를 52px로 — 임계점(64)에 두면 첫 카드 위에 얹혀 내용을 가린다. 실제로 렌더해 확인

## 3. 셸 연결

- [x] 3.1 `AppShell`의 `<main>`에 건다 — 화면마다 붙이면 새 탭에서 빠뜨린다 (D1)
- [x] 3.2 `refetchQueries({ type: 'active' })` — 무엇을 다시 부를지 화면마다 적지 않아도 된다
- [x] 3.3 본문은 옮기지 않는다 — `transform`이 걸리면 바텀시트가 화면이 아니라 본문 기준으로 뜬다 (D2)
- [x] 3.4 `renderWithAuth`에 `QueryClientProvider` 추가 — 셸이 클라이언트를 쓰게 됐다. 실제 App.tsx와 같은 구조다

## 4. 마무리

- [x] 4.1 테스트 8개 — 저항·임계점·미달·위로 올리기·맨 위 아님·시트 안·중복 호출·실패
- [x] 4.2 **가드가 실제로 잡히는지 확인** — `scrollY` 검사와 시트 검사를 빼서 각각 실패하는 것을 봤다.
      `window.scrollY = 200`이 jsdom에서 실제로 먹는 것도 단언으로 못 박았다(안 먹으면 헛통과다)
- [x] 4.3 `npm test` · `npm run build` · `openspec validate --strict` 통과
- [ ] 4.4 **실기기 확인** — 홈 화면 앱에서 당김이 iOS 고무줄과 안 싸우는지, 임계점 64px이 멀거나 가깝지 않은지,
      AI PT 대화·바텀시트에서 잘못 걸리지 않는지
