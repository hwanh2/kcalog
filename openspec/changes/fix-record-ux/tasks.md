# Tasks: fix-record-ux

> 전부 프론트엔드. PR 하나.
> 완료 판정은 "고쳤다"가 아니라 **테스트·빌드 통과 + 해당 시나리오 확인**이다.

## 1. 삭제 확인

- [x] 1.1 `ui/ConfirmSheet` — `Sheet` 기반, 제목·설명·취소/실행. 기본 선택은 취소, 파괴적 버튼은 강조하지 않는다 (D1)
- [x] 1.2 `MealCard` 삭제에 연결 — 무엇을 지우는지(음식 요약·칼로리) 보여준다
- [x] 1.3 삭제 버튼 탭 대상 44px + 수정 영역과 간격 확보
- [x] 1.4 테스트 — 확인 전에는 삭제 요청이 안 나감·취소하면 안 지워짐·Esc로 취소됨
- [x] 1.5 **중첩 확인 시트 테스트** — 분석 결과 시트 안에서 열려도 Esc가 확인만 닫는다 (D1 ⚠️)

## 2. 실패 알림

- [x] 2.1 뮤테이션 래퍼 훅 — 실패 시 메시지를 만들어 돌려준다. 메시지를 안 주면 기본 문구 (D2)
- [x] 2.2 즐겨찾기 저장·해제에 적용 (`AddFoodPanel`)
- [x] 2.3 기록 삭제·수정에 적용 (`MealCard`)
- [x] 2.4 체중 저장에 적용(`WeightPanel`) — `ProfileEditSheet`는 이미 성공/실패를 나눠 알리고 있어 그대로 뒀다(design "구현 이탈")
- [x] 2.5 나머지 뮤테이션 훑어 빠진 곳 없는지 확인 — `TdeeCard`(목표 적용)·`CoachPage`(대화 초기화)가 더 있었다. raw `useMutation`은 0개
- [x] 2.6 테스트 — 실패 시 `role="alert"`로 안내가 뜨고 화면 상태가 실제와 어긋나지 않음

## 3. 저장 실패 복구

- [x] 3.1 `RecordsPage`가 실패한 저장 요청을 들고 있는다 (items·mealType·analysisJobId) (D3)
- [x] 3.2 "다시 시도" — 같은 요청 재전송, `analysisJobId` 유지
- [x] 3.3 성공하면 보관한 요청을 비운다
- [x] 3.4 테스트 — 실패 후 재시도가 같은 항목·같은 `analysisJobId`로 나감(분석 횟수 재차감 없음)

## 4. 성공 문구 제거

- [x] 4.1 `AddFoodPanel`의 즐겨찾기 저장·해제 안내 제거 — ★ 변화가 곧 결과다 (D4)
- [x] 4.2 남은 성공 문구 점검 — 화면 상태로 드러나는 것은 문구를 두지 않는다
- [x] 4.3 테스트 — 즐겨찾기 저장·해제 후 안내 문구가 없다

## 5. 레이아웃 안정성

- [x] 5.1 사진 4곳에 종횡비로 자리 확보 (`AnalysisResultSheet`·`PhotoOverlay`·`AiRecordPanel`·`AnalyzingView`) (D5)
- [x] 5.2 세로로 비교되는 숫자에 `tabular-nums` — 기록 카드·합계·자주먹는·리포트
- [ ] 5.3 눈으로 확인 — 사진 로드 시 아래 버튼이 움직이지 않는다

## 6. 로딩·행동 유도

- [x] 6.1 `AnalyzingView`의 자리표시를 `ui/`로 꺼내 줄 수를 받게 (D6)
- [x] 6.2 `RecordsPage`·`AddFoodPanel`의 "불러오는 중…"을 스켈레톤으로
- [x] 6.3 기록 카드가 눌러 고칠 수 있음을 드러낸다

## 6-1. 음식 목록 중복 (백엔드)

> 구현 중 실제 화면에서 발견. 카탈로그 음식을 ★로 저장하면 사본과 원본이 둘 다 나왔다.

- [x] 6-1.1 `FoodService.findAll` — 즐겨찾기와 이름이 겹치는 카탈로그 항목 제외. 판정은 저장·조회와 같은 `FoodNames.normalize`
- [x] 6-1.2 기존 테스트가 버그를 굳혀놨던 것 수정 — `saveFavorite`이 30+1=31을 기대하고 있었다
- [x] 6-1.3 통합 테스트 4개 — 중복 없음·띄어쓰기 차이 흡수·해제 시 원본 복귀·카탈로그에 없는 음식은 그대로 추가

## 7. 마무리

- [x] 7.1 `npm test` · `npm run build` · `openspec validate --strict` 통과
- [x] 7.2 구현 중 설계와 어긋난 결정을 design.md에 반영
- [ ] 7.3 **실기기 확인** — 이번 변경 + `improve-ui-accessibility`에서 이월된 것(바텀시트 안전 영역·탭 지연·포커스 링). 에뮬레이터로는 안 잡힌다
