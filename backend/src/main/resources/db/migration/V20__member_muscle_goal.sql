-- 근육량 목표 여부. 탄단지 목표의 비율을 가른다(켜면 탄 45/단 30/지 25, 끄면 탄 55/단 20/지 25).
--   goal(CUT/MAINTAIN/BULK)로 대신할 수 없다: 그것은 체중이 어느 방향으로 갈지이고,
--   이 값은 그 체중을 근육으로 채우고 싶은지다. 감량하면서 근육을 지키려는 회원이 여기서 갈린다.
--   activity_level도 아니다. 이미 유지칼로리에 계수로 곱해져 있어 두 번 반영되고,
--   유산소만 하는 회원과 구분하지 못한다.
--   기존 회원은 백필하지 않는다. 이번 변경의 목적이 단백질을 낮추는 것이므로 기본값도 낮은 쪽에 둔다.
ALTER TABLE member
    ADD COLUMN muscle_goal BOOLEAN NOT NULL DEFAULT FALSE;
