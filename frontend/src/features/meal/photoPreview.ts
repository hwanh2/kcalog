/**
 * 고른 사진을 보여주는 크기 — **입력 단계와 분석 대기 화면이 같은 값을 써야 한다.**
 *
 * 두 화면은 같은 사진을 이어서 보여주는데, 크기가 다르면 분석으로 넘어가는 순간 사진이
 * 줄었다 늘었다 해서 다른 사진처럼 읽힌다. 실제로 대기 화면만 `w-52`(208px) + `object-cover`라
 * 사진이 작아지고 잘렸다.
 *
 * 각자 클래스를 적어두고 주석으로 "같이 고쳐라"라고 쓰는 대신 한 곳에서 정한다.
 *
 * `object-contain` — 찍은 사진의 비율을 모르므로 잘라내지 않고 담는다.
 */
export const PHOTO_PREVIEW_CLASS = 'block h-56 rounded-2xl object-contain'
